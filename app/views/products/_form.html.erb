<%= form_for (@product), :validate =>true do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation" >
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name , :placeholder => "Enter product name" %>
  </div>
  <div class="field">
    <%= f.label :rate %><br>
    <%= f.number_field :rate , :placeholder => "Enter rate" %>
  </div>
  <div class="actions">
    <%= f.submit :class => 'btn btn-success button_margin' , title: "Product List" %>
  </div>
<% end %>
